import {
  drawerWidth,
  transition,
  container
} from "../styles";

const layoutStyle = theme => ({
  wrapper: {
    position: "relative",
    top: "0",
    // height: "100vh",
    direction: "rtl",
    paddingBottom: 100
  },
  container
});

export default layoutStyle;
